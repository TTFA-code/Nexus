1. The ID Registry (The "Keys")Discord Guild ID (Test): 547362530826125313Discord Guild ID (Live): 1044236374401880074Data Type Standard: All IDs (guild_id, user_id, match_id) are now TEXT to prevent truncation.2. Database Schema (The "Backbone")TablePrimary KeyCritical Foreign KeysPurposeclubsguild_idN/AParent table for all servers.playersuuid_linkN/ACore player profiles & MMR.matchesidguild_id, creator_idRecords every game played.lobbiesidmatch_id, guild_idControls the "Live" Arena view.match_playersidmatch_id, user_idLinks players to specific matches.mmr_historyidplayer_uuid, match_idAudit trail for Elo changes.3. Core Logic Flows (The "Value Loops")Loop A: Match CompletionTrigger: Player submits score.Function: submit_match_reportAction: 1. Sets matches.status = pending_approval.2. Sets lobbies.status = finished (removes it from Arena).3. Clears players from queues.Loop B: Admin ApprovalTrigger: Admin clicks "Approve" in Command Center.Function: approve_match(match_id_input)Logic: 1. Calculates Elo shift ($K=32$, divisor=400).2. Updates players.mmr.3. Logs to mmr_history.4. Sets matches.status = completed.Loop C: Security GatePath: /dashboard/[guildId]/adminLogic: Gatekeeper.ts checks:Is the user the server owner in server_members?Does the user have the required Discord Roles (via Bot API)?4. Current Database Viewsadmin_match_review: Joins matches + game_modes + players.Filter: Only shows status = 'pending_approval'.Filter: Must match guild_id in URL.